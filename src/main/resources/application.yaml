server:
  port: ${SERVICE_PORT:8083}
spring:
  name: ${SERVICE_NAME:library-ui-service}
recommendation-service:
  rest:
    url: ${RECOMMENDATION_SERVICE_REST_URL:http://localhost:8082/graphql}
catalog-service:
  graphql:
    url: ${CATALOG_SERVICE_GRAPHQL_URL:http://localhost:8081/graphql}